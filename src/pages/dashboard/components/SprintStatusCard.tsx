// SprintStatusCard is now integrated directly into DashboardPage.
// This file is kept for backward compatibility but the component is no longer used.
// It can be safely deleted.

export function SprintStatusCard() {
 return null
}
